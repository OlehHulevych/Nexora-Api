using Nexora.Domain.Enums;

namespace Nexora.Domain.Constants;

public static class ActsNames
{
    public const string Increase = nameof(QuantityActs.INCREASE);
    public const string Reduce = nameof(QuantityActs.REDUCE);
}